{
 "path": "q-system/.q-system/scripts/coding-standards-lint.py",
 "reason": "narrowed to NEW files only (grandfathers anything present at merge-base) and MEASURED at 0 of 400 tracked files blocked, down from 36. It is still unarmed for one reason only: wiring it requires .claude/settings.json, and write access to .claude/ is refused by the harness permission layer -- the same wall the ASK-133 sentinel hit. settings-template.json alone is refused by settings-template-sync-check as skeleton-dead, so both files must move together. Not a design problem and not a blast-radius problem; a permission grant.",
 "spillover_id": "sp-994363e8"
}
