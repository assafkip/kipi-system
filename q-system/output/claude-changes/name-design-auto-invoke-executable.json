{
  "schema_version": 1,
  "slug": "name-design-auto-invoke-executable",
  "reason": "design-auto-invoke.md claims ENFORCED but names no executable, so prompt-only-enforcement-guard.py exits 2 on it at three lines (8, 10, 12 on origin/main). ASK-134 gave the public-vs-internal scoping decision a tested name, is_public_facing_page() in the kipi-design plugin's dogfood_gate.py, so the rule can now point at the code that actually makes the call. Branch A of the two the PR offered: name the executable. Branch B (drop the ENFORCED claim) was rejected because it removes a claim rather than making it true, and the executable exists and is wired.",
  "requires": {
    "files_present": [
      "plugins/kipi-design/hooks/dogfood_gate.py",
      "plugins/kipi-design/hooks/test_dogfood_gate.py"
    ]
  },
  "edits": [
    {
      "file": ".claude/rules/design-auto-invoke.md",
      "op": "insert_after",
      "anchor": "  - \"sites/**/*.jsx\"\n---",
      "insert": "\n\nThe scoping call is executable, not a judgement call left to the model:\n`is_public_facing_page()` in the kipi-design plugin's `dogfood_gate.py` hook decides\npublic-vs-internal from the path, and `test_dogfood_gate.py` is its test.",
      "reason": "Name the executable directly under the frontmatter. The guard clears a finding only when a deterministic term sits within two lines of it, and the finding on the frontmatter fence can only see this region."
    },
    {
      "file": ".claude/rules/design-auto-invoke.md",
      "op": "insert_after",
      "anchor": "Invoke design skills ONLY when building output that will be published or seen by the founder's audience (prospects, investors, users, public web).",
      "insert": "\n\n`is_public_facing_page()` in `dogfood_gate.py` is the deterministic half of the gate\ncheck below; a path it calls internal is skipped by the hook and needs no design pass.\nIt errs toward internal on purpose: a false block on a founder-only page (the GTM\ncockpit, ASK-134) gets the gate switched off, and a gate that is off protects nothing.",
      "reason": "Name the executable again beside the rule it governs, and record the ASK-134 direction-of-failure reasoning so the next edit does not tighten it back into a blocking false positive."
    }
  ]
}
