# Rework round 3 — both findings fixed, both reproduced red first

Head `38f5fed`. 15 new checks, all observed RED against PR head `de773ca` before the fix.

---

## MAJOR — a `started` issue whose PR merged is a permanent rework candidate, dispatched as FRESH work

**Confirmed, and the one link you flagged as code-inferred is now observed.** You said the Linear MCP permission was not granted, so you could not check whether real ASK issues sit in `started` post-merge. I ran the picker's own filters against live Linear through `linear-sync.py`:

```
total ASK issues: 246
rework candidates (owner:sana + DoR + started): 7
  ASK-245   open_pr=True  In Progress | fix(dispatch): an issue whose PR went red is never pick
  ASK-226   open_pr=True  In Progress | feat(triage): collapse the 49 duplicate-template issues
  ASK-225   open_pr=True  In Progress | feat(ops): dispatch picks file-disjoint issues so it ca
  ASK-223   open_pr=True  In Progress | feat(ops): a broken Linear pipeline pages the founder w
  ASK-221   open_pr=True  In Progress | feat(review): Codex reviews every PR as an independent
  ASK-218   open_pr=True  In Progress | feat(seam): the autonomous path writes a prd-os receipt
  ASK-150   open_pr=False In Progress | Constraint: cron cannot run `claude -p` (no keychain ac
```

ASK-150 is exactly your case: `started`, `owner:sana`, has a DoR, PR merged, nothing open on `sana/ask-150`. One of seven today, and permanent — your grep is right that no code path moves an issue out of `started` on merge.

It is also a DoR miss, not just a defect: the DoR says a candidate "has an **open PR** that is failing checks or unreviewed". The first cut dropped that clause.

### The fix, and where it landed

**`linear-worker.sh`** — the announcement requires an open PR, via `open_pr_for`, which is now the **single reader** of that question. The apply loop's inline `gh pr list --head` was replaced by the same helper rather than a second copy, because the picker comment's own argument ("two readers of `ready` with drifting semantics") applies to this question too.

**The report, not only the detector.** You caught the round-2 version of this and it applies here: the line the operator reads at 3am said `1 rework candidate(s)` for a pool member the run would then refuse. It now reports the pool as a pool and the survivors separately:

```
worker: 7 started issue(s) in the rework pool (owner:sana, has a DoR). Eligibility -- an OPEN PR, budget left -- is decided at the announcement.
[dry] skip rework ASK-150: no OPEN PR on sana/ask-150 -- it is In Progress with nothing to rework (merged and never closed, or the PR was closed). Not fresh work either; a human moves it off In Progress.
worker: 6 of 7 rework candidate(s) announced
```

**A `gh` failure refuses the path rather than falling through** (A9). "gh could not be asked" is not "there is no PR"; falling through would announce the whole pool — merged issues included — on exactly the run where nothing can be verified.

**`kipi-dispatch.sh` echoes the worker's refusals into `dispatch.log`.** The reasons live in `linear-worker.log`, which is not the file anyone opens when the loop looks idle.

### The bound — you are right that `MAX_ATTEMPTS` cannot do this

`bump_attempt` has one call site and it is on the non-zero-`claude` branch. The rework failure mode is an agent that exits **0** with the PR still red. Your A6 critique lands: it proves the filter works from a hand-written `{"count":3}`, a state this population cannot reach.

New fifth ledger key, `rework_dispatches`, capped at **2**, incremented **per dispatch** by `kipi-dispatch.sh` through a new `--rework-dispatched` worker mode. Notes on the shape:

- **The dispatcher does not write the ledger, it asks the worker to.** That file now has five keys answering five different questions and exactly one script has ever written it.
- **Fail closed:** no record written → no dispatch. Same rule as the budget counter it sits beside.
- **Lifetime, not consecutive**, unlike the conflict and drift caps — and stated in the comment. There is no state a reader could trust as "the streak ended": a rework dispatch *itself* moves the head, so a head-change clear would refill the budget on every single dispatch and cap nothing.
- At the cap it **pages once** and prints the one command that hands the issue back to the loop. A candidate that silently stops being dispatched is the failure this whole issue is named for.

### What got quieter, said explicitly

A started issue with **no PR at all** (worker died before pushing, or the PR was closed unmerged) is now picked up by neither set. It was never in the fresh set (state is `started`), and pre-ASK-245 it was invisible everywhere; what changes is that PR #43 as-shipped would have dispatched it *wrongly as fresh work*, and now it is a stated skip line instead. Nothing files or pages on that population — the honest home for that is a `fleet-health-daily.py` detector, not the dispatcher. Captured: `sp-88bfa1b5`.

---

## MINOR — the starvation comment overstates

Agreed, the sentence was wrong, not the code. Rewritten to say what is true: fresh work wins outright **within a tick**; across a budget day the two share `KIPI_DISPATCH_DAILY_MAX`, so a morning of rework can exhaust the day. That is the DoR's stated trade ("Not doing: raising the daily cap").

On your operator-facing note — *"Not an error, the loop is resting"* reading wrong for a day spent on no-ops — two things:

1. The no-op day is now gone at the source. A merged-PR candidate is not a candidate, and a red one is bounded at 2 dispatches, so a capped day cannot be four re-runs of one PR.
2. The cap line and page now name the split, so a rework-heavy day is legible rather than indistinguishable from a shipping day:
   ```
   DAILY CAP: 4/4 issues dispatched for budget day 2026-07-29 (1 fresh, 3 rework), stopping until 7:00 local
   ```
   A sibling file (`dispatch-rework-<day>`), not a field in `COUNT_FILE` — that file is parsed as a bare integer in three places, and not `$COUNT_FILE.rework` either, because the suite reads the budget through a `dispatch-count-*` glob.

---

## What I did not regress

The four attacks you ran and could not break are still asserted, and the ones your findings touch got stronger:

- **B4** (live converge, 5 runs, 0 dispatches, 0 budget) still passes. The rework-budget bump sits *after* the liveness guard, so a passed-over candidate spends nothing.
- **The `--issue` path** is still byte-identical: `only:` sets `rew = []`.
- **Set disjointness** (A2/A3b) unchanged.
- **A0** (the copy under test is byte-identical to the shipped worker) still passes, so none of this is graded against a stale copy.

One thing about the suite worth flagging, because it would have hidden the fix: the `gh` stub was `exit 0` with no output. Once the dry path asks `gh` a real question, that stub means "no PR" for every candidate and would have turned Part A green by announcing **nothing at all**. It now answers from two files (`$WORK/gh-open-branches`, `$WORK/gh-fail`) so each case states its world. And `work --rework-dispatched` in the Part B stub is **not** stubbed — it execs the real worker against the same ledger Part A reads, which is what makes **B9** a real end-to-end: the budget the dispatcher spends is the budget the picker refuses on.

## Evidence

```
RED  (against de773ca, new cases only failing):   28 passed, 15 failed
GREEN (head 38f5fed):                             43 passed, 0 failed

test-dispatch-liveness.sh        18 passed, 0 failed
test-linear-worker-fetch.sh      PASS (10 checks)
test-converge.sh                 PASS 17/17
test-linear-worker-parallel.sh   PASS (9 checks)
fleet-capability-verify.py       27 instances, 22 green, 0 red, 5 skipped
```

## Spillover opened this round (gate is RED on purpose, cleared at closeout)

- `sp-b9bc6528` — the apply loop's `EXISTING_PR=` still swallows `gh`'s exit code, so an outage there is indistinguishable from "no PR" and takes the fresh-work path. The announcement now checks rc; the apply loop does not. Pre-existing, unchanged by this issue.
- `sp-88bfa1b5` — the "started, no PR at all" population has no detector (above).
- `sp-53b02cc4` — `linear-worker-attempts.json` now has five unlocked read-modify-write bumpers, and this issue is the first to call one from a second *process*. Never concurrent for the same issue, but the class is real.
