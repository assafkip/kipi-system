ASK-133 — blocked on write permission for `.claude/` in this worker session.

MISSING CAPABILITY
  Write/Edit access to files under `.claude/` in the ask-133 worktree.
  Refused by the harness permission layer, twice, on two different files:
    - Edit `.claude/settings.json`
      -> "Claude requested permissions to write to ... but you haven't granted it yet."
    - Write `.claude/rules/coding-standards.md`
      -> "Claude requested permissions to edit ... which is a sensitive file."
  I did not route around either refusal (no Bash heredoc, no python open()).

WHY THIS IS A CAPABILITY BLOCK, NOT A SCOPE PROBLEM
  The DoR is correct and executable. Three of its six named files live under
  `.claude/`:
    - .claude/settings.json           (the PostToolUse switch)
    - .claude/rules/coding-standards.md  (the rule must name its executable)
    - .claude/rules/skill-hook-pairing.md ("Wired pairings" line)
  A perfectly re-written DoR would name the same three files, because that is
  where the wiring lives. Re-scoping burns a pass and returns the same block.

WHAT IS DONE AND SHIPPED ON THIS BRANCH
  - q-system/.q-system/scripts/coding-standards-lint.py (new, 4 rules,
    stdin hook contract + CLI mode, self-scoping, bypass marker)
  - q-system/.q-system/scripts/test_coding_standards_lint.py (new, 39 cases)
  - 35/39 green, including every behavior case and the full hook contract
    (exit 2 on a violating Write, exit 0 out of scope / non-edit tool /
    garbage stdin / missing file).
  - The 4 red are the Wiring class, and they are red for exactly one reason:
    the switch is not in either settings file and the rule does not name the
    executable, because those edits were refused. They go green the moment
    the four edits below land. They are deliberately NOT deleted.

WHAT UNBLOCKS IT (four edits, all mechanical)
  1. .claude/settings.json — PostToolUse "Edit|Write" cluster, beside
     lessons-validator.py:
       {"type":"command","command":"python3 \"$CLAUDE_PROJECT_DIR/q-system/.q-system/scripts/coding-standards-lint.py\"","timeout":5}
  2. settings-template.json — PostToolUse "Edit|Write|MultiEdit" cluster,
     beside lessons-validator.py:
       {"type":"command","command":"test -f \"$CLAUDE_PROJECT_DIR/q-system/.q-system/scripts/coding-standards-lint.py\" && python3 \"$CLAUDE_PROJECT_DIR/q-system/.q-system/scripts/coding-standards-lint.py\""}
     (I made this edit, saw settings-template-sync-check.py correctly fail it
     as skeleton-dead, and reverted it rather than commit a red gate. Both
     settings files have to move together.)
  3. .claude/rules/coding-standards.md — name the executable, split the four
     deterministic lines from the interpretive ones, and correct the
     "snake_case for scripts" line: the repo is 85 kebab vs 35 snake in
     q-system/.q-system/scripts alone, so enforcing that prose verbatim would
     block ~70% of script edits fleet-wide. The lint accepts both.
  4. .claude/rules/skill-hook-pairing.md — add
     "coding-standards (rule) -> coding-standards-lint" to Wired pairings.
  Then add to q-system/.q-system/capability-manifest.json expected_tests:
     {"path":"q-system/.q-system/scripts/test_coding_standards_lint.py","runner":"python3"}
  I held the manifest entry back on purpose: registering a suite that is
  knowingly red would turn the fleet capability gate red for everyone.

WHO OWNS IT
  Whoever configures this worker session's tool permissions. Grant the
  ask-133 worktree write access to `.claude/`, or land the four edits by hand.
