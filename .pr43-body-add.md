
---

# Round 3 (head `38f5fed`) — the reviewer's two findings

Full response: the round-3 comment on this PR.

**MAJOR, fixed.** A rework candidate must now have an **open PR**. Nothing in this repo moves an issue out of `started` when its PR merges, so every issue the loop has ever *finished* was landing in the pool — and with no open PR the apply loop's own `gh pr list --head` is equally empty, so the severity-floor gate was skipped, `BASE` stayed `origin/main`, and the agent got the **fresh-work** prompt for work already on main. Confirmed against live Linear (the reviewer could only infer it): **ASK-150**, `started`, `owner:sana`, DoR, PR merged, no open PR — 1 of 7 pool members on 2026-07-29. The DoR required this clause from the start; the first cut dropped it.

One shared `open_pr_for` reader serves the announcement and the apply loop. A `gh` failure refuses the path rather than announcing a pool it cannot verify. The **count line** reports the pool and the announced survivors separately, instead of calling a pool member a candidate.

**MAJOR, second half, fixed.** `MAX_ATTEMPTS` cannot bound this path: `bump_attempt` has one call site and it fires only on a **non-zero** `claude` exit, while the rework failure mode is an agent that exits 0 with the PR still red. New fifth ledger key `rework_dispatches`, capped at 2, incremented **per dispatch** by `kipi-dispatch.sh` through a new `--rework-dispatched` worker mode (one writer keeps the ledger schema in one script). Fail closed: no record, no dispatch. At the cap it pages once and prints the command that hands the issue back to the loop.

**MINOR, fixed.** The starvation comment claimed more than the code does — fresh work wins within a **tick**, not across a budget day. Restated. The daily-cap line and page now name the fresh/rework split, so a capped day says what it bought.

## Reproducer, updated

`q-system/.q-system/scripts/test/test-dispatch-rework.sh`, now **43 checks** (was 27). The 15 new ones observed **RED** against PR head `de773ca`:

```
RED  (de773ca):  28 passed, 15 failed
GREEN (38f5fed): 43 passed, 0 failed
```

A8/A9 (open-PR requirement, gh-failure refusal), A10/A11 (the dispatch cap, paged once), A12 (the count line tells the truth), B8 (a dispatch spends exactly one rework slot, a fresh one spends none), **B9** (the layer above: two real dispatches drive the budget to the cap, then the real picker refuses the candidate — either half alone leaves the loop unbounded).

The `gh` stub was `exit 0` with no output; once the dry path asks it a real question that stub would have turned Part A green by announcing nothing at all. It now answers from `$WORK/gh-open-branches` / `$WORK/gh-fail`. `work --rework-dispatched` in the Part B stub is **not** stubbed — it execs the real worker against the same ledger Part A reads.

## Spillover opened this round

`gates run` is RED on purpose until closeout: `sp-b9bc6528` (the apply loop still swallows `gh`'s exit code — pre-existing), `sp-88bfa1b5` (a started issue with **no PR at all** has no detector; this fix makes it a stated skip line rather than a wrong fresh-work dispatch), `sp-53b02cc4` (five unlocked read-modify-write bumpers on one ledger, now called from a second process).
