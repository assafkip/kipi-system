---
id: ASK-1824
title: design-chain: the gate test file runs all its tests, and the three stale gap-check tests test the seal that exists now
status: closed
priority: p1
parent_prd: linear:ASK-1824
allowed_files:
  - q-system/.q-system/scripts/test_design_chain_gate.py
disallowed_files:
  - q-system/.q-system/scripts/design-chain-gate.py
  - q-system/.q-system/scripts/test/stub_producers/design-gap-check.py
  - q-system/.q-system/capability/capability-gate.py
required_checks:
  - python3 q-system/.q-system/scripts/test_design_chain_gate.py
required_reviews: []
deliverables_count: 1
---
<!-- generated-by: prd_split.py prd=linear:ASK-1824 finding=linear-dor at=2026-09-19T02:11:39Z -->

# design-chain: the gate test file runs all its tests, and the three stale gap-check tests test the seal that exists now

## Context

Parent PRD: `https://linear.app/ask-consulting/issue/ASK-1824`

## Acceptance

the file has exactly one `unittest.main()`, at the end, and `python3 <file>` runs every test the file defines. The three `TestGapCheck` tests are rewritten to the seal-runs-the-producer model through the stand-in's own modes. (1) The producer writes no receipt (`STUB_GAP=silent`): seal refuses, naming `gap.json`. (2) The producer reports an axis below the floor (`STUB_GAP=below`): seal refuses, naming the axis. (3) A typed `gap.json` with any content (a passing one, and a stale sha) is never what seal reads: with the producer set to `silent` (it writes nothing, so the typed file is the only thing that could carry the seal), seal removes it and refuses. Each of the three names the producer branch's own message, and a mutant of the real gate kills each (seal ignores a missing receipt, ignores exit 2, never runs the producer, keeps a typed receipt). RED FIRST: the check run before the change shows the defined-vs-ran gap (the direct run stops at line 567). Verify contract 2 is the check's own proof: tests defined vs ran from verify's output, pasted in the closeout.

## Deliverables

<!-- Check each box when it ships; close refuses until checked count equals deliverables_count (locked at issue-start). -->
- [x] design-chain: the gate test file runs all its tests, and the three stale gap-check tests test the seal that exists now (one main() at the end; 86 of 86 run directly; the gap tests name the producer branch, 4 gate mutants killed)

## Amendments

### 2026-09-19T02:25:31Z
Reason: Review of c033a842: clause 3 named 'below', but only 'silent' proves the typed receipt is never read (below overwrites it anyway); and each gap test now names the producer branch's own message, pinned by four mutants of the real gate.

Before:
- allowed_files: ['q-system/.q-system/scripts/test_design_chain_gate.py']
- required_checks: ['python3 q-system/.q-system/scripts/test_design_chain_gate.py']
- disallowed_files: ['q-system/.q-system/scripts/design-chain-gate.py', 'q-system/.q-system/scripts/test/stub_producers/design-gap-check.py', 'q-system/.q-system/capability/capability-gate.py']

After:
- allowed_files: ['q-system/.q-system/scripts/test_design_chain_gate.py']
- required_checks: ['python3 q-system/.q-system/scripts/test_design_chain_gate.py']
- disallowed_files: ['q-system/.q-system/scripts/design-chain-gate.py', 'q-system/.q-system/scripts/test/stub_producers/design-gap-check.py', 'q-system/.q-system/capability/capability-gate.py']
