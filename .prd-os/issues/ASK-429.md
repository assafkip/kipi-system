---
id: ASK-429
title: Deferring an ISSUE finding must create a spillover item (fix shipped, wrapping retroactively)
status: open
priority: p1
parent_prd: linear:ASK-429
allowed_files:
  - plugins/kipi-dsse/scripts/issue_findings.py
  - plugins/kipi-dsse/scripts/test_issue_deferral_fanout.py
  - .claude/rules/no-orphan-findings.md
disallowed_files:
  - .prd-os/spillover.jsonl
  - plugins/prd-os/scripts/
  - q-system/
  - .git/
required_checks:
  - python3 -m pytest -q plugins/kipi-dsse/scripts/test_issue_deferral_fanout.py
required_reviews: []
deliverables_count: 1
---
<!-- generated-by: prd_split.py prd=linear:ASK-429 finding=linear-dor at=2026-08-06T19:12:18Z -->

# Deferring an ISSUE finding must create a spillover item (fix shipped, wrapping retroactively)

## Context

Parent PRD: `https://linear.app/ask-consulting/issue/ASK-429`

## Acceptance

Write the failing test FIRST and make it the one that actually failed: disposition an issue finding `deferred`, then assert an OPEN `defer-<issue>-<finding>` item exists. It must go red with an empty ledger, not an error. Then idempotency (deferring twice does not double-append), reverse transition (moving off `deferred` resolves the item), severity carry-over, and whole-body (no truncation). A `rejected` finding must create nothing — that is the control. Mutation-check the fan-out call site, including a mutant that disables it via a swallowed import, since a silently-disabled backstop is invisible at the CLI.

## Deliverables

<!-- Check each box when it ships; close refuses until checked count equals deliverables_count (locked at issue-start). -->
- [ ] Deferring an ISSUE finding must create a spillover item (fix shipped, wrapping retroactively)
