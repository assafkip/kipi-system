---
id: ASK-1814
title: receipts-ledger-check refuses every Linear-split issue's close: prd_id linear:ASK-n is not a plain slug
status: closed
priority: p1
parent_prd: linear:ASK-1814
allowed_files:
  - q-system/.q-system/scripts/receipts-ledger-check.py
  - q-system/.q-system/scripts/test/test-receipts-ledger-check.sh
disallowed_files:
  - plugins/prd-os/scripts/prd_split.py
  - plugins/kipi-dsse/scripts/issue_runner.py
  - **/lefthook.yml
required_checks:
  - bash q-system/.q-system/scripts/test/test-receipts-ledger-check.sh
required_reviews: []
deliverables_count: 1
---
<!-- generated-by: prd_split.py prd=linear:ASK-1814 finding=linear-dor at=2026-09-19T00:03:17Z -->

# receipts-ledger-check refuses every Linear-split issue's close: prd_id linear:ASK-n is not a plain slug

## Context

Parent PRD: `https://linear.app/ask-consulting/issue/ASK-1814`

## Acceptance

`prd_id` accepts exactly one extra form, the one the producer writes: `linear:` followed by a Linear identifier (`[A-Z][A-Z0-9]*-[0-9]+`), e.g. `linear:ASK-1808`. The form is anchored and names the key it applies to; every other `_id` key stays a plain slug. RED FIRST: the test's own "the repo's real committed ledger passes" case fails on a ledger holding the [ASK-1808](https://linear.app/ask-consulting/issue/ASK-1808/design-chain-seal-measures-a-snapshot-so-what-is-measured-is-what-is) row, before the fix. The test gains a positive case (`"prd_id":"linear:ASK-1808"` passes) and negative cases that must still be refused: `linear:` with free text (`linear:call acme`), with a lowercase or empty key (`linear:ask-1`, `linear:-1`), the form on another key (`"issue_id":"linear:ASK-1"`), and a different scheme (`jira:ASK-1`, `https://linear.app/...`). The header's "WHAT THIS DOES NOT DO" paragraph gains one sentence: a Linear identifier is a number, not a name, so it adds no semantic surface.

## Deliverables

<!-- Check each box when it ships; close refuses until checked count equals deliverables_count (locked at issue-start). -->
- [x] receipts-ledger-check refuses every Linear-split issue's close: prd_id linear:ASK-n is not a plain slug (prd_id accepts linear:<KEY>-<n>, key bounded to the producer's 10 chars, prd_id only)
