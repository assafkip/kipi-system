---
id: ASK-1811
title: design-chain: the chain reads the snapshot, not the disk
status: closed
priority: p1
parent_prd: linear:ASK-1811
allowed_files:
  - q-system/.q-system/scripts/design-chain-gate.py
  - q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py
  - q-system/.q-system/scripts/test/test_dc_served_round.py
  - q-system/.q-system/capability/expected_tests/q-system__.q-system__scripts__test__test_dc_chain_reads_snapshot.py.json
disallowed_files:
  - q-system/.q-system/scripts/design-standard-check.py
  - q-system/.q-system/scripts/design-gap-check.py
  - **/settings-template.json
  - .claude/settings.json
required_checks:
  - DC_REQUIRE_REAL_PRODUCERS=1 python3 q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py
required_reviews: []
deliverables_count: 1
---
<!-- generated-by: prd_split.py prd=linear:ASK-1811 finding=linear-dor at=2026-09-19T03:53:25Z -->

# design-chain: the chain reads the snapshot, not the disk

## Context

Parent PRD: `https://linear.app/ask-consulting/issue/ASK-1811`

## Acceptance

Every read of a round file after the snapshot is taken reads the bytes seal holds: seal hands `chain_problems()` the snapshot's copy of the page, so every in-round read, `craft_problems()` included, is rooted in the snapshot, and reads that leave the round map to the live tree through `_live()`, one function. While a seal holds a snapshot, `find_config()` for a path inside that snapshot returns the `design-chain.json` seal loaded (`snap.cfg_path`, the path the producers were given) and never walks again; when that is None the chain refuses "no design-chain.json found", as before. `round_dir_for()` never walks above the snapshot directory. The passive gate holds no snapshot and reads the disk, unchanged. At seal, a page with a logged correction or a sealed source declaration is held to the full chain like any other page: those exemptions are the passive gate's alone, because a seal writes a receipt and a receipt says the chain was measured. RED FIRST against the real gate, real producers and real chromium: an A -> X -> A watcher that swaps `brief.md` to a good brief at the moment seal's server comes up and restores the bad one after the verdict is written gives exit 0 and a receipt before the fix, and exit 2 with no receipt after; the same watcher on `craft-manifest.json`, injecting a wireframe declaration that exists on disk neither before nor after the seal, does not flip the producer selection. The same RED FIRST holds for: a round with no `brief.md` sealed while a complete fake round sits above the snapshot directory; a round sealed by a relative path from its parent; a round sealed through a symlink whose resolved tree carries a more permissive config. Both pre-fix and post-fix outputs are pasted in the closeout. The passive gate's behavior on an already-sealed round is unchanged, proved by running it over every round already sealed on disk. Not in this issue: the live-vs-snapshot compare ([ASK-1808](https://linear.app/ask-consulting/issue/ASK-1808/design-chain-seal-measures-a-snapshot-so-what-is-measured-is-what-is)), the receipt digest reader (dc-10), `round_declaration()` ([ASK-1808](https://linear.app/ask-consulting/issue/ASK-1808/design-chain-seal-measures-a-snapshot-so-what-is-measured-is-what-is)); reads of inputs outside the round (config bytes, owner files, OPEN-DECISIONS, the implements source, sibling briefs, exemplars, vision and specs, grounding file, references/): [ASK-1831](https://linear.app/ask-consulting/issue/ASK-1831).

## Deliverables

<!-- Check each box when it ships; close refuses until checked count equals deliverables_count (locked at issue-start). -->
- [x] design-chain: the chain reads the snapshot, not the disk (DELIVERED: seal checks the chain against the snapshot's page; round lookup and config are bounded to the held snapshot; outside-round reads map through _live(). Swap watchers on brief.md and craft-manifest.json: rc=0 + receipt before, rc=2 no receipt after. Consulting 32/32 COMPLETE. Moved: outside-round reads to ASK-1831.)

## Amendments

### 2026-09-19T04:00:03Z
Reason: Sana 2026-09-19: _seal_snapshot now calls chain_problems on the snapshot's page, so test_dc_served_round.py::OneReadOfTheBytes::test_seal_rechecks_the_bytes_immediately_before_it_writes_the_receipt (dc-03) wrote its 'window after the last check' change into the snapshot, which the final compare (files_that_differ against the live round) never sees. Adding the file; the stub now writes the LIVE page, restoring the test's intent through the mechanism that still enforces it.

Before:
- allowed_files: ['q-system/.q-system/scripts/design-chain-gate.py', 'q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py', 'q-system/.q-system/capability/expected_tests/q-system__.q-system__scripts__test__test_dc_chain_reads_snapshot.py.json']
- required_checks: ['DC_REQUIRE_REAL_PRODUCERS=1 python3 q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py']
- disallowed_files: ['q-system/.q-system/scripts/design-standard-check.py', 'q-system/.q-system/scripts/design-gap-check.py', '**/settings-template.json', '.claude/settings.json']

After:
- allowed_files: ['q-system/.q-system/scripts/design-chain-gate.py', 'q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py', 'q-system/.q-system/scripts/test/test_dc_served_round.py', 'q-system/.q-system/capability/expected_tests/q-system__.q-system__scripts__test__test_dc_chain_reads_snapshot.py.json']
- required_checks: ['DC_REQUIRE_REAL_PRODUCERS=1 python3 q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py']
- disallowed_files: ['q-system/.q-system/scripts/design-standard-check.py', 'q-system/.q-system/scripts/design-gap-check.py', '**/settings-template.json', '.claude/settings.json']

### 2026-09-19T04:37:39Z
Reason: Sana triage of review round 1 (40e6cd3d): P1 round_dir_for bounded by the snapshot, P2 find_config returns the config seal loaded, P4 corrected/sourced exemptions are passive-gate only (declared), RED FIRST extended to the three new cases, outside-round reads named as ASK-1831.

Before:
- allowed_files: ['q-system/.q-system/scripts/design-chain-gate.py', 'q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py', 'q-system/.q-system/scripts/test/test_dc_served_round.py', 'q-system/.q-system/capability/expected_tests/q-system__.q-system__scripts__test__test_dc_chain_reads_snapshot.py.json']
- required_checks: ['DC_REQUIRE_REAL_PRODUCERS=1 python3 q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py']
- disallowed_files: ['q-system/.q-system/scripts/design-standard-check.py', 'q-system/.q-system/scripts/design-gap-check.py', '**/settings-template.json', '.claude/settings.json']

After:
- allowed_files: ['q-system/.q-system/scripts/design-chain-gate.py', 'q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py', 'q-system/.q-system/scripts/test/test_dc_served_round.py', 'q-system/.q-system/capability/expected_tests/q-system__.q-system__scripts__test__test_dc_chain_reads_snapshot.py.json']
- required_checks: ['DC_REQUIRE_REAL_PRODUCERS=1 python3 q-system/.q-system/scripts/test/test_dc_chain_reads_snapshot.py']
- disallowed_files: ['q-system/.q-system/scripts/design-standard-check.py', 'q-system/.q-system/scripts/design-gap-check.py', '**/settings-template.json', '.claude/settings.json']
